# Skip WebFetch Preflight

Merge into `~/.claude/settings.json`:

```json
{
  "skipWebFetchPreflight": true
}
```

Restart Claude Code.

## Requirements

- Claude Code build that supports `skipWebFetchPreflight`
